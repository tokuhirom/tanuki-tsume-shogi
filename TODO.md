# TODO

## 緊急（バグ）

- [ ] `src/shogi-core.js` を `docs/shogi-core.js` と同期する
  - src版に桂馬(N)・香車(L)のサポートが欠落している
  - `findBestDefense()` が src版に存在しない
  - `validate.js` が src版を使うため、N/L を含むパズルの検証が失敗する
  - 理想的には shogi-core.js を一箇所に統合し、docs/ からも scripts/ からも参照する構成にする
- [ ] `forward()` 関数が未使用（src/docs 両方）→ 削除する

## 機能改善

- [ ] 問題数の上限撤廃
  - 現在100問に制限しているが、生成可能ならもっと収録する
  - 生成パラメータ（attempts）の調整も検討
- [ ] 持ち駒の表示を改善
  - 現在ボタン形式で不自然。一般的な詰将棋アプリのように盤面横に表示する
- [ ] 駒の表示をリッチにする
  - Unicode 将棋駒文字（☗☖ や絵文字駒）の活用を検討
- [ ] 3手・5手の面白さ改善
  - 打ち・成り・捨て駒を含む問題を優先採用する重みを調整する
  - 同型問題の連続出題をさらに抑制する
- [ ] 検証エンジンの精度改善
  - 唯一解判定を全分岐で厳密化する
  - 検証ロジックのテストケースを追加する
- [ ] プレイ画面の操作改善
  - 手入力を合法手ベースで厳密チェックする

## 開発環境

- [ ] oxlint / oxfmt を導入（JSのリント・フォーマット）
- [ ] cargo clippy を CI に追加
- [ ] キャッシュバスト値 `?v=20260307b` をビルド時に動的生成する
- [ ] E2E テストに N/L 駒の持ち駒ラベル対応を追加

## 後続

- [ ] （後続）7手詰・9手詰対応
  - 逆算探索ベースで検証済み問題を収集する

## 完了済み

- [x] 成り選択UIの実装
- [x] 問題リンク共有とURL復元（`?mate=...&id=...`）
- [x] GitHub Actions のCI/Pagesデプロイ設定
- [x] Playwright E2E 追加（3手#1のクリアまで検証）
- [x] 成り選択UIをモーダル化して視認性を上げる
- [x] スマホ向けタップ領域と誤タップ対策を改善する
- [x] `generate -> validate -> build:info` の一括コマンドを追加する（`npm run build`）
- [x] README に公開URLを追記する
- [x] 攻め方の駒構成が同じパズルの連続を抑制（attacker_composition_key + 特徴量重み強化）
- [x] 持ち駒選択時の盤面表示範囲を合法打ち先に基づいて計算（全盤表示を回避）
- [x] CI で E2E・Rustテスト・構文チェック・パズル検証を常時実行
